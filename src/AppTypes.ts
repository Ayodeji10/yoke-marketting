export interface MapProps {
  longitude: number;
  latitude: number;
  zoom: number;
}

export interface Locaton {
  name: string;
  lat: number;
  lon: number;
  today: string;
  tomorrow: string;
}
